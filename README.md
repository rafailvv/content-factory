# Контент Завод
Telegram бот для автоматической публикации контента. Собирает популярные посты из указанных каналов, переписывает их в заданном стиле, генерирует изображения и публикует по расписанию.

## Возможности

- 📥 Автоматический сбор постов из нескольких Telegram каналов
- 🎯 Выбор наиболее популярных постов на основе просмотров, реакций и репостов
- ✍️ Переписывание текста в заданном стиле с помощью AI
- 🎨 Генерация изображений на основе текста
- ⏰ Публикация постов по расписанию
- 📊 Учет базовых показателей каналов для более точного отбора контента

## Установка

1. Клонируйте репозиторий или скачайте файлы проекта

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

## Настройка

1. Создайте файл `.env` в корне проекта со следующим содержимым:

```env
API_ID=your_telegram_api_id
API_HASH=your_telegram_api_hash
SESSION_NAME=your_session_name
TELEGRAM_BOT_TOKEN=your_bot_token
```

### Как получить API_ID и API_HASH:

1. Перейдите на https://my.telegram.org/apps
2. Войдите в свой аккаунт Telegram
3. Создайте новое приложение
4. Скопируйте `api_id` и `api_hash`

### Как получить TELEGRAM_BOT_TOKEN:

1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте токен бота

### Настройка каналов и стилей:

Откройте `config.py` и настройте:

- `source_channels` - список каналов-источников (например: `["mosguru", "moscowmap"]`)
- `target_channel` - канал для публикации (например: `"@moscow_actual_news"`)
- `text_style` - стиль переписывания текста (например: `"Дружелюбный и информативный тон"`)
- `image_style` - стиль генерации изображений (например: `"Реалистичные изображения"`)
- `post_schedule_utc` - расписание публикаций в UTC (например: `["20:17", "15:00"]`)

## Запуск

```bash
python main.py
```

Бот запустится и начнет работать по расписанию. Для немедленной проверки можно раскомментировать строки 67-68 в `main.py`:

```python
print("Выполняется поиск...")
await do_post(cfg)
```

## Структура проекта

```
content-factory-1/
├── main.py              # Главный файл, точка входа
├── config.py            # Конфигурация и настройки
├── fetcher.py           # Получение постов из Telegram каналов
├── selector.py          # Выбор лучших постов на основе метрик
├── rephraser.py         # Переписывание текста и генерация промптов для изображений
├── image_generator.py   # Генерация изображений
├── scheduler.py         # Планировщик публикаций
├── requirements.txt     # Зависимости проекта
└── README.md           # Документация
```

## Как это работает

1. **Fetcher** собирает посты из указанных каналов за последний период
2. **Selector** анализирует посты и выбирает наиболее популярный на основе:
   - Просмотров
   - Реакций
   - Репостов
   - Базовых показателей канала
3. **Rephraser** переписывает текст в заданном стиле
4. **ImageGenerator** создает изображение на основе переписанного текста
5. Бот публикует пост с изображением в целевой канал по расписанию

## Зависимости

- `telethon` - для работы с Telegram API
- `aiogram` - для работы с Telegram Bot API
- `python-dotenv` - для загрузки переменных окружения
- `requests` - для HTTP запросов к AI API
- `pillow` - для обработки изображений
- `apscheduler` - для планирования задач

## Используемые сервисы

- **Pollinations.ai** - для переписывания текста и генерации изображений
  - Text API: `https://text.pollinations.ai/`
  - Image API: `https://image.pollinations.ai/prompt/`

## Примечания

- Бот работает в часовом поясе UTC
- Изображения автоматически обрезаются (удаляется нижние 60 пикселей)
- Посты без текста игнорируются
- При отсутствии постов за период публикация не выполняется

## Лицензия

Проект создан для личного использования.
